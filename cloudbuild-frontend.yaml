steps:
  - name: node:22
    dir: Frontend
    env:
      - VITE_API_BASE_URL=${_VITE_API_BASE_URL}
    script: |
      npm ci
      npm run build
  - name: gcr.io/cloud-builders/gsutil
    args:
      - -m
      - rsync
      - -r
      - -d
      - Frontend/dist
      - gs://${_FRONTEND_BUCKET}

substitutions:
  _VITE_API_BASE_URL: https://todo-api-1035557533758.europe-west3.run.app
  _FRONTEND_BUCKET: todo-cloud

options:
  logging: CLOUD_LOGGING_ONLY
